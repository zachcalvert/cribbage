version: '3.7'

services:
  flask:
    build: ./server
    volumes:
      - ./server/:/code
    ports:
      - 5000:5000
    env_file:
      - server/env.dev
    depends_on:
      - redis
  redis:
    image: redis